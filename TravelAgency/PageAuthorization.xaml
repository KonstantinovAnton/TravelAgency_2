<Page x:Class="TravelAgency.PageAuthorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TravelAgency"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAuthorization">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="5*"></RowDefinition>
        </Grid.RowDefinitions>
        

            <TextBlock FontSize="40">Авторизация</TextBlock>

       
            <StackPanel Grid.Row="1" Background="AliceBlue">
            <TextBlock HorizontalAlignment="Center">Логин</TextBlock>
            <TextBox Name="textBoxLogin"  Width="100"></TextBox>
            <TextBlock HorizontalAlignment="Center">Пароль</TextBlock>
            <PasswordBox  Name="passBox" Width="100"></PasswordBox>
            <Button Name="btnAuthorizate" Click="btnAuthorizate_Click" Width="100" Margin="350,0" >Войти</Button>

        </StackPanel>
              </Grid>
</Page>
